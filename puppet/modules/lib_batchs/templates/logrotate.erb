<%= @batchs_root %>/log/*.log {
    daily
    rotate 180
    missingok
    notifempty
    compress
    sharedscripts
    postrotate
        /bin/kill -USR1 $(cat <%= @batchs_root %>/batchs.pid) 2>/dev/null || :
    endscript
}
